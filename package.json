{
  "name": "minispector-log-lite",
  "version": "1.0.0",
  "private": true,
  "description": "MiniSpector Log — lean rebuild: web-only, always-online, single Express service + Railway Postgres",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "migrate": "node server/migrate.js",
    "test": "node --test test/**/*.test.js"
  },
  "dependencies": {
    "docx": "^9.7.1",
    "docxtemplater": "^3.69.3",
    "express": "^4.19.2",
    "pg": "^8.11.5",
    "pizzip": "^3.2.0"
  }
}
